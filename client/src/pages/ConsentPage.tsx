import { ComingSoonPage } from './ComingSoonPage'

export function ConsentPage() {
  return (
    <ComingSoonPage
      title="Consent Management"
      description="Pending access requests, active grants, and access history land in a follow-up feature issue — see the project's open issues for the consent management work."
    />
  )
}
